package Generator;

import Application.ZKeyboard;

/*  CLASS Keyboard EXTENDS ZKeyboard
 *  @Zanice
 * 
 * 	This implemented class handles key events for the application,
 * 	though it is not used since no inputs from the keyboard are
 * 	used in the application (besides typing in new values, which
 * 	is done through TextWindow).
 */

public class Keyboard extends ZKeyboard {
	public Keyboard() {
		super();
	}
}
